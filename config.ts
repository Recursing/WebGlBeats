// Deno Deploy signaling server URL
export const SIGNALING_SERVER = "https://huge-weasel-83.recursing.deno.net";

// Polling interval for signaling (ms)
export const POLLING_INTERVAL_MS = 1000;

// Device orientation send throttle (ms)
export const SEND_INTERVAL_MS = 20;
